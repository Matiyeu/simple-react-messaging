import React, { Component } from 'react'

class Loading extends Component {
  render () {
    return (
    <p className={'text-center'}><i className={'fas fa-spinner fa-spin fa-3x'} /></p>
    )
  }
}

export default Loading
